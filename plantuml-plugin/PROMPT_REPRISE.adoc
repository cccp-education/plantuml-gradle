= PROMPT_REPRISE — Plantuml — Session 219
:session: 219
:date: 2026-09-10

== État — PUB 0.0.5 terminé (S-218)

Session 218 : publication Maven Central du HEAD réel. Le Central 0.0.4
était **périmé** (POM `workspace-bom:0.0.19`, dernier code `8b75c59`
2026-08-03, pré-RENAME-5/RENAME-11) — le HEAD était nulle part sur Central.

* MEMPHIS : `workspace-bom` 0.0.38 + `workspace-catalog` 0.0.38 publiés
  (bundle `e816302c`, AUTOMATIC) avec `plantuml-plugin = "0.0.5"`.
* plantuml-plugin **0.0.5** publié (bundle `36e2614e`, AUTOMATIC), POM
  importe `workspace-bom:0.0.38`, repo1 HTTP 200.
* Contenu 0.0.5 : HEAD complet (PLT-BOUNDARY, i18n rattrapage, RENAME-5
  `256e10d`, RENAME-11 `c958077`, FIX-BASELINE S-217 TEST-only).
* Consommateur `document-plugin` : catalogue ws 0.0.38 + BOM 0.0.38 +
  `plantuml-plugin = "0.0.5"` — compileKotlin VERT.
* Version dérivée : `ws.versions.plantuml.plugin` = 0.0.5 (toml l.2 aligné,
  garde D5 `PlantumlPluginPublicationTest` satisfait).
* Gate : `:plantuml-plugin:check` VERT (unit + cucumber). functionalTest
  `File permissions` ROUGE restant = bug `FilePermission` S-030 préexistant
  (vérifié par stash) — **hors périmètre PUB**.
* Clôture : BACKLOG EPIC PUB réécrit (0.0.5), synchronisation gouvernance
  en cours.

== Mission session 219 — Maintenance DORMANT

. **PL-3** — ktlint ou detekt (BLOQUÉ ABIDJAN).
. Reste `functionalTest` : `PlantumlFunctionalSuite$FilePermission`
   (1 FAIL, préexistant connu S-030) — hors cucumber, périmètre FIX-BASELINE
   n'ayant pas été étendu à functionalTest.
. Si besoin cross-borough (document, bakery, slider, codex) → baby-step
   DDD/TDD/BDD.
. Sinon → maintenance (audit gouvernance, rotation backup si multiple de
   10 — prochaine S-220).

== Backlog — priorisé

[cols="1,4,1"]
|===
| Priorité | Tâche | Est.
| ✅ PUB 0.0.5 | ✅ TERMINÉ (S-218) — publication HEAD réel, consommateur aligné, BOM 0.0.38 | ~1 session
| 🟡 PL-3 | ktlint ou detekt | BLOQUÉ ABIDJAN
|===

== Méthodologie

Baby-step DDD/TDD/BDD (unit → functional → cucumber), agile software
craftsmanship. Red→Green→Refactor, 1 test à la fois.

== Rappels

- GroupId: education.cccp.plantuml — version dérivée `ws.versions.plantuml.plugin` (catalog 0.0.38 = 0.0.5)
- Central: 0.0.1, 0.0.2, 0.0.4 (périmé), 0.0.4→ **0.0.5 publié S-218** (HEAD réel)
- BOM: `workspace-bom` platform 0.0.38 ✅ (S-218)
- Conventions: `education.cccp.build.*:0.0.4` ✅
- `publishToMavenLocal` obligatoire après modif code plugin
- Kover main: 83.26% INSTRUCTION ✅ ≥ 75%
- Cucumber: baseline **VERTE** (FIX-BASELINE S-217)
- Functional: 1 FAIL préexistant `FilePermission` (connu S-030)
- Configuration Cache: activé ✅
- Tous les EPICs métier terminés — RENAME-5 ✅ (S-216), FIX-BASELINE ✅ (S-217), PUB 0.0.5 ✅ (S-218)
- Plugin dormant — maintenance uniquement