= PROMPT_REPRISE — Plantuml — Session 218
:session: 218
:date: 2026-09-10

== État — FIX-BASELINE terminé (S-217)

Session 217 : les 3 échecs cucumber **préexistants** de HEAD sont résolus —
la baseline cucumber est désormais **VERTE**.

* Attempt History Tracking (2 FAIL) : cause racine = regex `"valid"`
  minuscule vs champ réel `"isValid"` (V majuscule, case-sensitive) →
  `"(?:is)?Valid"` × 4 dans `AttemptHistorySteps.kt`. Suite 3/3 VERT.
* RAG Pipeline (1 FAIL) : fixture écrivait `generated/rag` (relatif) alors
  que `CollectPlantumlIndexTask` lit `build/plantuml-plugin/generated/rag`
  → "No RAG directory found". Dir corrigé + nouveaux steps dédiés
  (`startPgvectorContainerWithEmbeddings` + `...WithUnchangedPromptDiagram`
  avec seed checksum). `@rag` 4/4 PASSED (log `Skipped rag-test.puml`
  confirmé).
* Correctifs **TEST-only** (zéro fichier production, zéro bump BOM) →
  `publishToMavenLocal` non requis.
* Gates : `@rag` BUILD SUCCESSFUL 6m44s ; `@plantuml` BUILD SUCCESSFUL
  (XML Attempt History tests=3 failures=0, +3ème ré-exécution verte après
  crash infra d'1 test-worker).
* Commit + push clôture (permission pilote).

== Mission session 218 — Maintenance DORMANT

. **PUB 0.0.3** — Publication Maven Central plantuml-plugin (3 US
   PUB-0.0.3-1/2/3). Version code = 0.0.4 (dérivée `ws.versions.plantuml.plugin`),
   Central = 0.0.1/0.0.2. ~1 session. Sur demande pilote.
. **PL-3** — ktlint ou detekt (BLOQUÉ ABIDJAN).
. Reste `functionalTest` : `PlantumlFunctionalSuite$FilePermission`
   (1 FAIL, préexistant connu S-030) — hors cucumber, périmètre FIX-BASELINE
   n'ayant pas été étendu à functionalTest.
. Si besoin cross-borough (document, bakery, slider, codex) → baby-step
   DDD/TDD/BDD.
. Sinon → maintenance (audit gouvernance, rotation backup si multiple de
   10 — prochaine S-220).

== Backlog — priorisé

[cols="1,4,1"]
|===
| Priorité | Tâche | Est.
| ▢ FIX-BASELINE | ✅ TERMINÉ (S-217) — échecs cucumber préexistants résolus | ~1 session
| 🟡 PUB 0.0.3 | Publication Maven Central plantuml-plugin (3 US) — version actuelle dérivée ws catalog 0.0.4 | ~1 session
| 🟡 PL-3 | ktlint ou detekt | BLOQUÉ ABIDJAN
|===

== Méthodologie

Baby-step DDD/TDD/BDD (unit → functional → cucumber), agile software
craftsmanship. Red→Green→Refactor, 1 test à la fois.

== Rappels

- GroupId: education.cccp.plantuml — version dérivée `ws.versions.plantuml.plugin` (catalog 0.0.30 = 0.0.4)
- Central: 0.0.1, 0.0.2 publiés. Version code: 0.0.4 (à publier)
- BOM: `workspace-bom` platform 0.0.30 ✅ (MEM-CAT-ROLLOUT S-030)
- Conventions: `education.cccp.build.*:0.0.4` ✅
- `publishToMavenLocal` obligatoire après modif code plugin
- Kover main: 83.26% INSTRUCTION ✅ ≥ 75%
- Cucumber: baseline désormais **VERTE** (FIX-BASELINE S-217 résoud les 3 FAIL préexistants)
- Functional: 1 FAIL préexistant `FilePermission` (connu S-030)
- Configuration Cache: activé ✅
- Tous les EPICs métier terminés — RENAME-5 ✅ (S-216), FIX-BASELINE ✅ (S-217)
- Plugin dormant — maintenance uniquement