= COMPLETED_TASKS_ARCHIVE — 2026-09

== Session 218 (2026-09-10) — PUB 0.0.5 : publication Maven Central HEAD réel ✅

=== Fait
- Audit POM Central 0.0.4 : périmé (workspace-bom 0.0.19, code 8b75c59 pré-RENAME) → HEAD nulle part sur Central ✅
- MEMPHIS workspace-bom/workspace-catalog 0.0.38 publiés (bundle e816302c, AUTOMATIC), plantuml-plugin=0.0.5 ✅
- plantuml-plugin 0.0.5 publié (bundle 36e2614e, AUTOMATIC), POM workspace-bom 0.0.38, repo1 200 ✅
- plantuml-plugin : settings catalog 0.0.38 + BOM 0.0.38 + toml local version 0.0.5 (garde D5) ✅
- Consommateur document-plugin : catalogue ws 0.0.38 + BOM 0.0.38 + plantuml-plugin 0.0.5, compileKotlin VERT ✅
- BACKLOG EPIC PUB réécrit (correction du faux « 0.0.4 already on Central ») ✅
- Gate : :plantuml-plugin:check VERT ; functionalTest FilePermission ROUGE = S-030 préexistant hors périmètre ✅
- Zéro bug introduit (bumps uniquement) ✅

== Session 216 (2026-09-08) — RENAME-5 : Neutralisation registre non-traductible ✅

=== Fait
- RENAME-5 exécuté en baby-step TDD (test d'abord, Red→Green) : `"FPA"` → `"QUALIOPI"` (référence publique française, pattern D5 cadrage) ✅
- `NonTranslatableTermRegistryTest.kt` : fixture bulk registration `"FPA"` → `"QUALIOPI"` ✅
- `NonTranslatableTermRegistryLoaderTest.kt` : YAML fixture `"FPA"` → `"QUALIOPI"` ✅
- `NonTranslatableTermRegistryLoader.kt` : KDoc exemple YAML `"FPA"` → `"QUALIOPI"` ✅
- Gate grep cadrage : `rg -ni "fpa|cda" src/` = 1 match = faux positif `ofPattern` (`DiagramProcessor.kt:302`, prévu au cadrage) ✅
- Unit boundary ciblés : 13/13 PASSED ✅
- `./gradlew check` : `test` VERT + `functionalTest` baseline exacte (1 FAIL `FilePermission` préexistant S-030) + `cucumberTest` 131/3 FAIL préexistants stash-confirmés (Attempt History ×2, RAG Pipeline ×1) ✅
- `publishToMavenLocal` VERT (règle INDEX 0 — KDoc prod modifié) ✅
- Gouvernance : rapport 216 + PROMPT_REPRISE 217 + SESSIONS_HISTORY + INDEX + TEST_COVERAGE_ANALYSIS + archive 2026-09 créée + BACKLOG (FIX-BASELINE ajouté) ✅
- Zéro bump BOM, zéro secret, zéro changement sémantique
- Commit + push (clôture pilote)

=== Prochaine session
- Session 217 — maintenance DORMANT OU diagnostic échecs baseline cucumber (FIX-BASELINE, sur demande pilote). Prochaine rotation backup au multiple de 10 (session 220).
== Session 217 (2026-09-10) — FIX-BASELINE : échecs cucumber préexistants résolus ✅

=== Fait
- Attempt History Tracking (2 FAIL) — cause racine : regex `Regex("\"valid\"...")` minuscule vs champ réel `"isValid"` (V majuscule, case-sensitive) → jamais matché. Fix : regex `"(?:is)?Valid"` × 4 dans `AttemptHistorySteps.kt` ✅
- RAG Pipeline scénario 3 (1 FAIL) — cause racine : fixture écrivait dans `generated/rag` (relatif) alors que `CollectPlantumlIndexTask` lit `build/plantuml-plugin/generated/rag` → "No RAG directory found" → early return, jamais de skip. Fix : dir corrigé + nouveau step dédié "a pgvector container with an unchanged prompt diagram" (seed checksum `build/plantuml-plugin/checksums/rag-test.sha256` = SHA-256 de `prompts/rag-test.prompt`, même format `ChecksumStore`) ✅
- Séparation fixtures RAG : scénario 2 ("existing embeddings") reçoit le dir corrigé SANS checksum (évite la régression "prompt skip" de `generatePlantumlDiagrams` découverte en cours de session) ; scénario 3 a son propre Given avec checksum ✅
- Gate `@rag` : BUILD SUCCESSFUL in 6m44s, 4/4 PASSED, log `→ Skipped rag-test.puml (source prompt unchanged)` confirmé ✅
- Gate `@plantuml` : BUILD SUCCESSFUL in 3m19s, XML Attempt History tests="3" failures="0" (1er run a crashé un test-worker infra → feature ré-exécutée, X run vert) ✅
- Baseline cucumber désormais VERTE (FIX-BASELINE TERMINÉ). zéro fichier production modifié, zéro bump BOM, zéro secret. Correctifs TEST-only → `publishToMavenLocal` non requis ✅
- Gouvernance : rapport 217 + PROMPT_REPRISE 218 + SESSIONS_HISTORY + INDEX + archive 2026-09 + sync BACKLOG ✅
- Commit + push (clôture pilote)

=== Prochaine session
- Session 218 — maintenance DORMANT OU PUB 0.0.3 (publication Maven Central plantuml-plugin, version actuelle dérivée ws catalog 0.0.4) OU PL-3 (ktlint/detekt, BLOQUÉ ABIDJAN). Prochaine rotation backup au multiple de 10 (session 220).
